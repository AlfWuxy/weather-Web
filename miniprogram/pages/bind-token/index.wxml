<view style="padding: 16px;">
  <view class="card">
    <view style="font-size: 18px; font-weight: 700;">绑定 API Token</view>
    <view class="muted" style="margin-top: 8px;">
      试点阶段先在网页端生成 Token，在这里输入完成绑定。
    </view>

    <view style="margin-top: 14px;">
      <input
        placeholder="粘贴 Web 生成的 Token"
        value="{{tokenInput}}"
        bindinput="onInput"
        style="border: 1px solid #e5e7eb; border-radius: 10px; padding: 10px 12px; background: #fff;"
      />
    </view>

    <view style="display:flex; gap: 10px; margin-top: 14px;">
      <button class="btn-primary" style="flex:1;" bindtap="onBind">绑定并进入</button>
      <button class="btn-outline" style="flex:1;" bindtap="onClear">清空</button>
    </view>

    <view class="muted" style="margin-top: 10px; font-size: 12px;">
      注意：小程序请求域名必须配置 HTTPS 合法域名，且与后端一致。
    </view>
  </view>

  <view style="height: 12px;"></view>

  <view class="card">
    <view style="font-weight: 700; font-size: 16px;">网页也能打卡</view>
    <view class="muted" style="margin-top: 6px; font-size: 12px;">
      扫码进入本页是为了绑定 Token。老人确认仍在网页短码页完成，小程序本批没有打卡。
    </view>
    <view style="margin-top: 8px; font-size: 13px;">{{webActionUrl}}</view>
    <view style="margin-top: 12px;">
      <button class="btn-outline" bindtap="copyWebAction">复制网页打卡地址</button>
    </view>
  </view>
</view>

