<view class="page-pad">
  <view class="card">
    <view class="page-title">绑定 API Token</view>
    <view class="muted page-lead">
      试点阶段先在网页端生成 Token，在这里输入完成绑定。
    </view>

    <view class="field-lg">
      <input
        class="input"
        placeholder="粘贴 Web 生成的 Token"
        value="{{tokenInput}}"
        bindinput="onInput"
      />
    </view>

    <view class="btn-row">
      <button class="btn-primary btn-flex" bindtap="onBind" disabled="{{busy}}">绑定并进入</button>
      <button class="btn-outline btn-flex" bindtap="onClear" disabled="{{busy}}">清空</button>
    </view>

    <view class="muted hint">
      注意：小程序请求域名必须配置 HTTPS 合法域名，且与后端一致。
    </view>
  </view>
</view>
