<view style="padding: 16px;">
  <view class="card">
    <view style="font-size: 18px; font-weight: 700;">预警</view>
    <view class="muted" style="margin-top: 6px; font-size: 12px;">
      官方预警来自 QWeather。天气未更新时不展示温度阈值。
    </view>
    <view class="muted" style="margin-top: 10px;">
      地点：{{location.query || '-'}}
    </view>
    <view wx:if="{{weather.weather_available}}" class="muted" style="margin-top: 4px; font-size: 12px;">
      Tmax {{weather.temperature_max}} · Tmin {{weather.temperature_min}}
    </view>
    <view wx:else class="muted" style="margin-top: 4px; font-size: 12px;">
      天气更新中，阈值暂不可用
    </view>
  </view>

  <view style="height: 12px;"></view>

  <block wx:if="{{loading}}">
    <view class="muted" style="text-align:center; padding: 20px;">加载中…</view>
  </block>

  <block wx:if="{{!loading && warnings.length === 0}}">
    <view class="muted" style="text-align:center; padding: 20px;">暂无官方预警</view>
  </block>

  <block wx:for="{{warnings}}" wx:key="title">
    <view class="card" style="margin-bottom: 12px;">
      <view style="font-weight: 700;">{{item.title || '预警'}}</view>
      <view class="muted" style="margin-top: 6px;">{{item.type || ''}} {{item.level || ''}}</view>
      <view style="margin-top: 8px; line-height: 1.6;">{{item.text || ''}}</view>
      <view class="muted" style="margin-top: 8px; font-size: 12px;">
        {{item.start_time || ''}} {{item.end_time ? ('→ ' + item.end_time) : ''}}
      </view>
    </view>
  </block>

  <view class="card" style="margin-top: 6px;">
    <view class="muted" style="font-size: 12px;">
      说明：本工具提供通用行动建议，不提供医疗诊断/治疗建议；如明显不适请及时就医。
    </view>
  </view>
</view>

